# LEDGER — view-consumer-gaps

| ID | Date | Decision | Type | Rationale | Evidence required to revisit |
|----|------|----------|------|-----------|------------------------------|
| **L-001** | 2026-09-08 | **DIRECTIVE R1: adopted** — Owner instruction in chat ("start the fixes per your recommendation", following the analysis of TODO.md T-1..T-5 the Owner had just read: order T-3 → T-1 → T-4, with T-2/T-5 as design entries only). DIRECTIVE.md transcribes that instruction; Authority APPROVED on the Owner's chat approval. | APPROVED | The Owner adopted a specific, already-analyzed scope; the directive records it rather than inventing one. | Owner disputes the transcription (any OC not matching the adopted recommendation) → amend via R2. |
| **L-002** | 2026-09-08 | **Proportionate artifact set**: BRIEF/CONTRACT/LEDGER/PLAN (+ DIRECTIVE, `.blueprint-status`, `.blueprint-execution`), with **PLAN.md as the Directive Completeness carrier** in place of TRACEABILITY_MATRIX.md. Follows `blueprints/INDEX.md` header convention and repository-write-lifecycle **D7** precedent; CONTRACT.md carries the API-surface duty for the new symbols. | APPROVED | A fix batch with additive-only API does not warrant the 9-artifact LIBRARY ceremony; the premise requires the contract be written and test-pinned, not heavyweight artifacts (D7's own rationale). | The wave grows beyond the listed scope (new module, breaking API, or multi-release effort) → full artifact set. |
| **L-003** | 2026-09-08 | **T-3 approach: fsLib sibling wrapper, not a consumer patch and not waiting for upstream.** Ship `FsTomSelectRemoteInput` (extends KVision's `TomSelectRemoteInput`, overrides `refreshState` + `loadResults` with an in-flight per-value guard that clears on completion *and on failure*) and `FsTomSelectRemote` (mirror of `TomSelectRemote`, which hard-wires its input as `final override val` — subclass swap impossible) + `fsTomSelectRemote` / `fsTomSelectRemoteInput` DSL builders. Upstream fix to KVision recommended separately (rjaros/kvision; same class as PR #584 precedent). **Retirement condition (FsTabPanel precedent): when a KVision release fsLib adopts guards `refreshState` against in-flight loads, retire the wrapper** — the guard unit test doubles as the tripwire only if upstream also changes `loadResults` visibility/behavior; otherwise re-check `TomSelectRemoteInput.refreshState` sources at every KVision bump (noted in CONTRACT I-3). | APPROVED | The defect multiplies every form-open in every consumer (measured ×7-9); consumers patching individually re-pays the cost per app; upstream latency is unbounded (measured defect identical in 9.5.0 → 9.6.0). | KVision release with an in-flight guard in `TomSelectRemoteInput.refreshState` → retire `FsTomSelectRemote*`. |
| **L-004** | 2026-09-08 | **T-1 approach: `I18n.tr` on all four dialog strings** (caption, text, `yesTitle`, `noTitle` — KVision `Confirm` defaults "Yes"/"No" are raw, verified in 9.6.0 sources). **Rejected:** caption/text as `ViewItem` constructor/params — instance fix, duplicates what i18n already solves, and fsLib already uses `tr()` (toolBarList.kt). | APPROVED | The established pattern exists in-repo; consumers translate via catalog (mppArel adds 4 keys to `messagesEs`). | A consumer that cannot ship a catalog needs per-view overrides → reopen the params alternative. |
| **L-005** | 2026-09-08 | **T-4 approach: provider overload + point-of-use `tr()`.** New `helpButtons(viewClassName, viewLabelProvider: () -> String, ...)` carries the implementation; the existing `String` overload delegates with `{ viewLabel }` (source + binary compatible). Captions computed inside the open/click handlers. All user-facing literals in `helpButtons.kt` wrapped in `tr()` with English sources. **Spanish `HelpType`/`HelpTheme` enum labels in `:core` are wrapped at point of use (`tr(helpType.label)`) — the keys stay Spanish**, an accepted impurity: changing enum values is consumer-visible and `:core` commonMain cannot depend on kvision-i18n; relocating labels out of the enums is its own decision, deferred. | APPROVED | Smallest change that makes every string translatable and the label live; no caller breaks. | A second consumer language ships and the Spanish keys prove confusing in catalogs → open the label-relocation decision. |
| **L-006** | 2026-09-08 | **T-2 design entry (implementation DEFERRED — Owner call pending).** Defect: `ViewList.tabulatorInfoPersistenceConfig = true` persists `field/title/width/visible` + column order; renamed/moved columns never reach browsers with a stored layout (mppArel B-2, workaround `LAYOUTS_OBSOLETOS`). Options: **(a)** limit persistence to `persistence.columns = ["width","visible"]` — simple, order still persists (Tabulator stores the layout array), titles unfreeze; behavior change for all consumers; **(b)** per-view `layoutVersion` invalidation — simple, nukes user widths on every bump; **(c) reconcile-by-field (recommended)**: on restore, drop persisted columns whose `field` no longer exists, insert new fields at code position, always take `title` from code, keep user `width`/`visible`/order — code is the source of truth for structure, persistence keeps user preferences. Note: consumers can already set the companion `var` today without an fsLib change. | DEFERRED | The remedy choice is a consumer-visible behavior trade-off — Owner call; recording it prevents re-measuring. | Owner picks an option → PLAN item + DIRECTIVE amendment (new OC). |
| **L-007** | 2026-09-08 | **T-5 design entry (implementation DEFERRED — Owner call pending; mppArel unblocked via its L-106 app bus).** Gap: `ConfigViewItem.openViewItem(modal)` offers no close/write notification. Proposed `openViewItem(apiItem, vmode, onClosed: ((ItemState<T>?) -> Unit)? = null)` receiving the last *complete* write (fsLib's `onUpsertResult` contract, `ViewItem.kt:334`) or null when closed clean. **Open semantics question**: a single `onClosed` conflates closed-clean / saved-once / saved-N-times (last wins) — acceptable for refresh-on-close consumers; per-write consumers keep overriding `onUpsertResult`. Alternative: separate `onWrite` stream + `onClosed`. | DEFERRED | Downstream is unblocked; the semantics choice deserves an Owner decision, not a default. | Owner picks the semantics → PLAN item + DIRECTIVE amendment (new OC). |
| **L-008** | 2026-09-09 | **DIRECTIVE R2: ACS pre-commit advisory adopted in full (ACS-01..06, Owner disposition "ADOPT", chat 2026-09-09).** What changed: (1) **ACS-01** — `InFlightValueGuard` is set-based (a single slot forgot outstanding loads on A→B→A value changes, admitting a duplicate A load); A→B→A regression test added to OC-01's coverage set (scope/cost impact: none — same jsTest suite, OC meaning unchanged). (2) **ACS-02** — the guard field is `lateinit` created via `::field.isInitialized`, so the guard built during the parent constructor's `loadResults` dispatch survives subclass initialization and the preset-value load is guarded too; the PLAN's constructor-load exception is removed and CONTRACT I-1 is unconditional. (3) **ACS-03** — request parameters (including `stateFunction`) are prepared BEFORE the guard is marked, so a synchronous throw can no longer wedge a marker. (4) **ACS-04** — `View.startDisplayPage` passes `viewLabelProvider = { label }`, fixing the motivating stale-caption defect for fsLib's own standard views (not only migrated consumers). (5) **ACS-05** — the offcanvas theme dropdown's three raw-DOM label interpolations route through `gettext`; the OC-03 check now sweeps `.label` interpolations, not only literals. (6) **ACS-06** — records reconciled: OC-02/OC-03 and CONTRACT I-4 state the real i18n split (`gettext` immediate / `I18n.tr` whole-string markers — L-004/L-005's and BRIEF's "`tr()`"/"`I18n.tr`" wording stands as historical shorthand for "through KVision i18n", superseded in detail by this row); `InFlightValueGuard` made **internal** (off the public API); P1.2's "refused" wording corrected (the drop lives in `refreshState`); the migration note no longer claims the String overload fixes staleness. | APPROVED | The advisory's findings were each verified against the code before adoption; fixing the guard beats weakening the contract, and the View wiring is what makes the first wave actually fix the measured symptom. | A KVision release guards `refreshState` upstream → L-003's retirement condition governs; any future OC wording change → R3. |
| **L-009** | 2026-09-09 | **Second ACS round disposed (Owner, chat 2026-09-09): ACS-01 ADOPT · ACS-02 ADOPT WITH CHANGES · ACS-03 ADOPT.** (1) **ACS-01** — `detachToWindow` no longer interpolates the title into `document.write`: the shell is written with an empty `<title>` and an empty `.detached-help-bar-title` span, then both are filled via `document.title` / `textContent`. Rationale: since the View wiring (L-008/ACS-04) the title carries the live item label — database content — into a same-origin popup; text insertion closes the markup-injection path while the help HTML body stays HTML by design. (2) **ACS-02, bounded fixture attempt taken and its fallback exercised**: a component fixture (fake `RpcServiceMgr` — trivial, two-method interface; real `CallAgent` fetch against the Karma server) DID drive the real constructor dispatch and duplicate-drop wiring, but any dispatched load's eventual RPC failure surfaces as an **uncaught global error that Karma/Mocha attributes to whichever test is running** — a run showed 4 failures including 2 unrelated `PaginationCounterTest` cases. The rethrow is deliberate upstream parity (upstream's coroutine dies uncaught the same way); changing failure semantics to caught+logged would make the fixture trivial but is its own design decision, NOT taken here. **Fixture withdrawn. The integration boundary's standing oracles are: (a) the mppArel 1-RPC consumer measurement (already an OC-01 obligation), (b) the `TomSelectRemoteInput.refreshState` source re-check at every KVision bump (CONTRACT I-3), (c) `InFlightValueGuardTest` for the state machine.** Reopen trigger: if the failure path is ever redesigned as caught+logged (own decision), write the fixture then — it becomes deterministic. (3) **ACS-03** — PLAN P1.3/P1.4/P1.5 mechanism wording corrected to the real i18n split; full-blueprint sweep run; BRIEF's shorthand covered by L-008's supersession note. | APPROVED | Each remedy verified in code; the fixture fallback was pre-authorized in the disposition and its attempt evidence recorded so nobody re-runs the same experiment blind. | ACS-01: none (strictly safer). ACS-02: failure-path redesign → write the fixture. ACS-03: none. |
